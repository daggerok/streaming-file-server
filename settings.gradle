rootProject.name = "streaming-file-server"
include "modules:app"
include "modules:docker"

//def modulesDir = file("$rootProject.projectDir.absolutePath/modules")
//
//modulesDir.listFiles().each { File build ->
//  if (!build.isDirectory()) return
//  includeBuild build
//}
